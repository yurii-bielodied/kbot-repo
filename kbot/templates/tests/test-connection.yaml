apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kbot.fullname" . }}-test-connection"
  labels:
    {{- include "kbot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kbot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
